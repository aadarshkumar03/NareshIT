# Q8.Write a python program, which will calculate a^m, here a is called base and m is called power. #HW
# basepow.py

# Taking Inputs
a=float(input("Enter Base Value:"))
m=int(input("Enter Power Value:"))

# Calculate a^m
print("{} to the power of {} = {}".format(a,m,a**m))
